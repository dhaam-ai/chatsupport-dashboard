apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatsupport-dashboard
  labels:
    app: chatsupport-dashboard
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chatsupport-dashboard
  template:
    metadata:
      labels:
        app: chatsupport-dashboard
    spec:
      containers:
      - name: chatsupport-dashboard
        image: <IMAGE>
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: chatsupport-dashboard
spec:
  selector:
    app: chatsupport-dashboard
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chatsupport-dashboard
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
  - host: dev-nexus.dhaamai.com
    http:
      paths:
      - path: /cs-dashboard(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: chatsupport-dashboard
            port:
              number: 80
